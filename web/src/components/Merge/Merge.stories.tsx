import Merge from './Merge'
import {standard} from "src/components/Merge/Merge.mock";

export const generated = () => {
  return <Merge  {...standard()} />
}

export default { title: 'Components/Merge' }
